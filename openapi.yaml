openapi: 3.0.3
info:
  title: Synapse API
  description: |
    OpenAPI design specification for the Synapse API platform.

    > **Note:** This specification represents the **"to be"** (target) state of the API,
    > not the current ("as is") implementation.
  version: 0.1.0
  contact:
    name: SummaCollege
    url: https://github.com/SummaCollege/SynapseAPI

servers:
  - url: https://api.example.com/v1
    description: Production server (placeholder)

paths: {}

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
